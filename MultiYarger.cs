using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MultiYarger : MonoBehaviour
{
    /* This script checks multiYarg input from main Yarger on equipped player and checks it with any custom multiYargs that
     * may be attached to the skin. It also runs the custom Yarg animation if one exists
     */

    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
